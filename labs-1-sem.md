# Лабораторная работа 0. Паспорт игрового профиля

## Сложность

1 / 5

## Темы

Первая программа, ввод и вывод данных, переменные, строка, преобразование строки в `int`.

## Задание

Практически каждая современная онлайн-игра предлагает игроку создать профиль при первом запуске.

Разработайте программу, которая позволяет заполнить карточку нового игрока.

Пользователь должен указать информацию о себе, после чего программа должна сформировать аккуратно оформленный игровой профиль, который можно показать другому человеку.

Какие именно сведения необходимо запросить, определите самостоятельно. Однако среди них обязательно должны присутствовать как текстовые данные, так и числовые.

Формат оформления итоговой карточки также определяется самостоятельно.

## Подсказки

<details>
<summary>Показать подсказки</summary>

- Для получения данных от пользователя можно использовать `Console.ReadLine()`.
- Любые данные, введённые пользователем, сначала являются строками.
- Если введённое значение должно использоваться как число, его потребуется преобразовать в тип `int`.
- Постарайтесь сделать итоговую карточку максимально удобной для чтения.
- Для оформления можно использовать пустые строки, символы `|`, `-`, `=`, табуляцию (`\t`) и другие символы.

</details>

## Дополнительное задание

Подумайте, какую ещё информацию можно было бы добавить в профиль игрока, чтобы он выглядел интереснее, и реализуйте эту возможность.

# Лабораторная работа 1. Мониторинг игрового сервера

## Сложность

1 / 5

## Темы

Типы данных, числовые типы данных, логический тип данных, преобразование и приведение типов данных, приоритет типов данных, переменные, операторы, арифметические операции, строка.

## Задание

Вы разрабатываете небольшую консольную утилиту для администратора игрового сервера.

Перед началом каждого игрового дня администратор собирает основные характеристики сервера, чтобы убедиться, что он готов принимать игроков.

Разработайте программу, которая получает информацию о состоянии сервера и формирует удобную сводку.

Какие именно характеристики анализировать и какие дополнительные показатели вычислять — определите самостоятельно. Постарайтесь сделать так, чтобы итоговая сводка действительно помогала быстро оценить состояние сервера.

Результат должен быть понятен человеку, который будет использовать программу ежедневно.

## Подсказки

<details>
<summary>Показать подсказки</summary>

- Исходные данные можно получать как из консоли, так и из текстового файла.
- Для чтения из консоли можно использовать `Console.ReadLine()`.
- Для чтения файла можно использовать `File.ReadAllLines()`.
- Любые данные, считанные из файла, сначала являются строками.
- Если необходимо выполнять вычисления, строки потребуется преобразовать в числовые типы данных.
- Подумайте, какие данные должны храниться как целые числа, какие — как вещественные, а какие — как логические значения.
- При вычислениях с вещественными числами учитывайте особенности их хранения и сравнения.
- "Удобная сводка" может представлять собой красиво оформленный вывод в консоль с использованием символов `|`, `-`, `+`, табуляции (`\t`) и других элементов оформления.
- Другой вариант — сохранить результат в отдельный файл, например в формате CSV или любом другом формате, который удобно обрабатывать программам.
- Если результат предназначен человеку, главным критерием должна быть читаемость. Если программе — удобство дальнейшей обработки данных.

</details>

## Дополнительное задание

Добавьте возможность сравнить текущее состояние сервера с предыдущим запуском и вывести краткое заключение о том, улучшилась ли ситуация или ухудшилась.

# Лабораторная работа 2. Проверка конфигурации сервера

## Сложность

1 / 5

## Темы

Операторы ветвления, логический тип данных, операторы, методы, строка, рефакторинг и улучшение кода.

## Задание

Перед запуском игрового сервера автоматически выполняется проверка его конфигурации.

Некоторые параметры могут конфликтовать друг с другом. В отдельных случаях сервер можно запускать только после предупреждения администратора, а некоторые конфигурации делают запуск невозможным.

Разработайте программу, которая получает сведения о конфигурации сервера, анализирует их и определяет один из возможных результатов:

- сервер готов к запуску;
- сервер можно запустить, но необходимо предупредить администратора;
- запуск невозможен.

Какие именно параметры используются при проверке и какие правила между ними существуют — определите самостоятельно. Постарайтесь сделать проверку максимально правдоподобной.

Все сообщения программы должны быть понятны человеку, который отвечает за работу сервера.

## Подсказки

<details>
<summary>Показать подсказки</summary>

- Перед написанием программы полезно выписать все возможные ситуации на бумаге.
- Некоторые проверки могут зависеть сразу от нескольких параметров.
- Если в разных местах программы начинают повторяться одинаковые действия, подумайте, можно ли сделать решение более понятным.
- Хорошее сообщение не просто сообщает об ошибке, а помогает понять её причину.

</details>

## Дополнительное задание

Добавьте возможность выводить сразу все найденные проблемы конфигурации, а не только первую обнаруженную.

# Лабораторная работа 3. Генератор отчётов для Discord

## Сложность

2 / 5

## Темы

Методы, строка, специальные символы, экранирование, методы и свойства строки, значение `null`.

## Задание

После завершения игрового события администратор публикует отчёт в Discord.

Чтобы не составлять сообщение вручную каждый раз, необходимо разработать программу, которая автоматически формирует готовый текст отчёта на основе введённых данных.

Какая именно информация должна входить в отчёт — определите самостоятельно. Например, это могут быть название события, дата проведения, количество участников, победитель, продолжительность, призовой фонд, дополнительные комментарии и другие сведения.

Если часть информации отсутствует, итоговое сообщение должно оставаться понятным и аккуратно оформленным.

Структуру сообщения и стиль оформления определите самостоятельно. Получившийся текст должен быть готов к публикации без дополнительного редактирования.

## Подсказки

<details>
<summary>Показать подсказки</summary>

- Большую задачу обычно проще разделить на несколько небольших.
- Подумайте, какие части программы можно оформить как отдельные методы.
- У класса `string` есть множество полезных методов и свойств, которые могут упростить обработку текста.
- Некоторые символы невозможно записать в строку напрямую — для этого используются специальные последовательности символов.

</details>

## Дополнительное задание

Добавьте возможность выбирать один из нескольких шаблонов оформления отчёта, например краткий и подробный.

# Лабораторная работа 4. Анализ журнала событий сервера

## Сложность

2 / 5

## Темы

Циклы, операторы перехода, методы, рефакторинг и улучшение кода.

## Задание

Во время работы игровой сервер ведёт журнал событий. Каждая строка журнала содержит одно событие: подключение игрока, отключение, успешный вход, ошибку, запуск карты, сохранение мира и т.д.

Необходимо разработать программу, которая анализирует журнал событий и формирует итоговый отчёт о работе сервера.

Какие именно сведения должны попасть в отчёт, определите самостоятельно. Например, это могут быть наиболее частые события, количество ошибок, число подключений игроков, время наиболее высокой активности или другие полезные характеристики.

Журнал может содержать произвольное количество записей.

После получения рабочего решения проанализируйте свой код и постарайтесь сделать его более понятным и удобным для сопровождения, не изменяя поведение программы.

## Подсказки

<details>
<summary>Показать подсказки</summary>

- Количество строк в журнале заранее неизвестно.
- Не каждая запись журнала обязательно представляет интерес при формировании итогового отчёта.
- Если в программе появляются большие повторяющиеся фрагменты кода, подумайте, как сделать решение проще и понятнее.

</details>

## Дополнительное задание

Добавьте возможность игнорировать некоторые типы событий. Какие именно события можно исключить из анализа, определите самостоятельно.

# Лабораторная работа 5. Менеджер сохранений игры

## Сложность

2 / 5

## Темы

Файл, каталог, кодировка файла, методы, строка.

## Задание

Компьютерная игра хранит информацию о сохранениях в отдельных файлах. Со временем их становится слишком много, поэтому игроку становится сложно находить нужное сохранение и поддерживать порядок.

Разработайте программу, которая помогает управлять сохранениями игры.

Какие возможности должна предоставлять программа — определите самостоятельно. Например, это может быть просмотр существующих сохранений, создание резервных копий, поиск сохранений по определённым признакам, восстановление резервной копии, удаление устаревших файлов или другие полезные функции.

Структуру каталогов и формат хранения данных также определите самостоятельно. Программа должна корректно работать с текстовыми файлами и папками.

## Подсказки

<details>
<summary>Показать подсказки</summary>

- Перед началом работы полезно заранее продумать структуру каталогов, в которых будут храниться основные файлы и резервные копии.
- Если программа работает с текстовыми файлами, обратите внимание на используемую кодировку.

</details>

## Дополнительное задание

Добавьте возможность автоматически создавать журнал всех выполненных операций (например, создание, удаление, восстановление или резервное копирование сохранений) в отдельном текстовом файле.

# Лабораторная работа 6. Мониторинг сервера в реальном времени

## Сложность

3 / 5

## Темы

Массив, виды массивов, передача массива в метод, сравнение массивов и строк, методы.

## Задание

Во многих системах мониторинга информация о состоянии сервера отображается в виде графиков, которые обновляются в режиме реального времени.

В рамках этой лабораторной необходимо разработать упрощённую версию такого монитора.

Вам будет предоставлен готовый класс, который имитирует работу настоящего сервера и позволяет получать текущие значения:

- загрузки процессора;
- использования оперативной памяти;
- температуры процессора.

Каждую секунду программа должна получать новые данные, сохранять историю измерений и отображать её в виде графиков, которые обновляются в режиме реального времени.

Количество одновременно отображаемых измерений определите самостоятельно.

Способ отображения графиков также выбирается самостоятельно.

## Использование готовой библиотеки

Для этой лабораторной вам предоставляется небольшая библиотека, имитирующая работу настоящего сервера. Создайте отдельный файл и скопируйте в него предоставленный код. Также внизу будет пример использования этой библиотеки в вашем коде.

**Важно**

Исходный код этой библиотеки использует конструкции языка, которые вы ещё не изучали. Это нормально.

В данной лабораторной **не нужно разбираться, как она устроена**. Достаточно знать, какие методы предоставляет библиотека и что они возвращают.

Это похоже на использование готовой библиотеки, найденной в интернете или подключённой через NuGet.

<details>
<summary>Код библиотеки</summary>

```csharp
using System;
using System.Timers;

public class ServerMonitor
{
    private readonly Random _random = new();

    private readonly Timer _timer;

    private double _cpu = 35;
    private double _ram = 42;
    private double _temperature = 48;

    public event Action<double, double, double>? DataReceived;

    public ServerMonitor()
    {
        _timer = new Timer(1000);
        _timer.Elapsed += (_, _) => GenerateData();
    }

    public void Start()
    {
        _timer.Start();
    }

    public void Stop()
    {
        _timer.Stop();
    }

    private void GenerateData()
    {
        _cpu += _random.NextDouble() * 10 - 5;
        _ram += _random.NextDouble() * 6 - 3;
        _temperature += _random.NextDouble() * 4 - 2;

        _cpu = Math.Clamp(_cpu, 0, 100);
        _ram = Math.Clamp(_ram, 10, 100);
        _temperature = Math.Clamp(_temperature, 30, 95);

        DataReceived?.Invoke(
            Math.Round(_cpu, 1),
            Math.Round(_ram, 1),
            Math.Round(_temperature, 1));
    }
}
```

</details>

Использовать библиотеку можно следующим образом.

<details>
<summary>Пример использования</summary>

```csharp
<details>
<summary>Пример использования</summary>

```csharp
using System;

internal class Program
{
    private static void Main()
    {
        // Создаём объект, который будет получать данные о состоянии сервера.
        var monitor = new ServerMonitor();

        // Сообщаем библиотеке, какой метод нужно вызывать
        // каждый раз, когда появляются новые данные.
        monitor.DataReceived += OnDataReceived;

        // Запускаем получение данных.
        // После этого библиотека сама будет передавать новые значения примерно раз в секунду.
        monitor.Start();

        Console.WriteLine("Мониторинг запущен.");
        Console.WriteLine("Для завершения программы нажмите Enter.");

        // Не даём программе завершиться сразу после запуска.
        Console.ReadLine();

        // При желании можно остановить получение данных.
        monitor.Stop();
    }

    private static void OnDataReceived(double cpu, double ram, double temperature)
    {
        // Этот метод автоматически вызывается библиотекой
        // каждый раз, когда появляются новые данные.

        // cpu — текущая загрузка процессора (в процентах).
        // ram — использование оперативной памяти (в процентах).
        // temperature — температура процессора (в градусах Цельсия).

        Console.WriteLine($"CPU: {cpu}%");
        Console.WriteLine($"RAM: {ram}%");
        Console.WriteLine($"Temperature: {temperature} °C");

        // Вместо этих строк вы можете написать свой код.
        // Например:
        // - сохранить значения в массивы;
        // - обновить график;
        // - вычислить статистику;
        // - вывести информацию в другом формате.
    }
}
```

</details>

## Подсказки

<details>
<summary>Показать подсказки</summary>

- Для хранения истории измерений удобно использовать массивы.
- Подумайте, сколько последних измерений имеет смысл отображать одновременно.

<details>
<summary>Подсказка: как можно нарисовать график</summary>

Одним из возможных вариантов является построение графика с помощью обычных текстовых символов.

Например, если за последние 8 минут были получены следующие значения загрузки процессора:

```
20 35 45 60 55 70 68 50
```

График может выглядеть так:

```text
70 |           █
60 |         █ █
50 |       █ █ █   █
40 |     █ █ █ █   █
30 |   █ █ █ █ █   █
20 | █ █ █ █ █ █ █ █
   +-----------------
     1 2 3 4 5 6 7 8
```

Это лишь один из возможных вариантов оформления. Вы можете разработать собственный способ отображения графика.

Не обязательно перерисовывать график после каждого нового измерения.

Например, библиотека передаёт новые данные каждую секунду, а график можно обновлять один раз в минуту. В этом случае по оси X будет отображаться история за последние несколько минут, а не за последние несколько секунд.

Для обновления изображения можно воспользоваться методом `Console.Clear()`. Однако полная очистка экрана может вызывать заметное мерцание. Если вам удастся обновлять только изменившуюся часть изображения, результат будет выглядеть значительно лучше, хотя это и не является обязательным требованием.

</details>

</details>

## Дополнительное задание

Реализуйте возможность самостоятельно выбирать масштаб графика. Например, одна колонка может соответствовать 10 секундам, 1 минуте, 5 минутам или другому интервалу времени. График должен автоматически перестраиваться в соответствии с выбранным масштабом.

# Лабораторная работа 7. Отладка рекурсивного алгоритма

## Сложность

3 / 5

## Темы

Рекурсия, дерево рекурсии, рефакторинг и улучшение кода, отладка.

## Задание

Один из разработчиков начал писать программу, которая строит дерево каталогов по специальному текстовому описанию.

Например, файл

```text
A

BEGIN B
BEGIN C
END

BEGIN D
END
END

BEGIN E
END
```

должен быть преобразован в

```text
A
├─B
│ ├─C
│ └─D
└─E
```

Однако программа работает неправильно:

- некоторые каталоги выводятся не на своём уровне вложенности;
- часть дерева отображается неверно;
- код трудно читать и сопровождать.

Ваша задача:

1. С помощью отладчика найти причину неправильной работы программы.
2. Исправить найденные ошибки.
3. Улучшить читаемость программы, не изменяя её поведения.
4. Подготовить краткое описание найденных ошибок и выполненных изменений.

Во время выполнения задания рекомендуется активно использовать режим пошаговой отладки и окно **Call Stack** (Стек вызовов).

## Подсказки

<details>
<summary>Показать подсказки</summary>

- Перед началом отладки попробуйте нарисовать на бумаге, как программа должна обходить дерево.
- При пошаговом выполнении обращайте внимание на значения локальных переменных.
- Во время выполнения программы откройте окно **Call Stack**. Оно поможет увидеть, какие вызовы методов ещё не завершились.
- Если программа неожиданно начинает выводить элементы не на том уровне вложенности, попробуйте проследить, в какой момент меняется глубина дерева.

</details>

## Дополнительное задание

После исправления программы добавьте вывод количества дочерних элементов рядом с каждым каталогом.

Например:

```text
A (2)
├─B (2)
│ ├─C (0)
│ └─D (0)
└─E (0)
```

<details>
<summary>Программа для исправления</summary>

```cs
using System;
using System.IO;

internal class Program
{
    // Current line in file
    private static int _lineIndex;

    // Current nesting level
    private static int _depth;

    // File content
    private static string[] _lines = null!;

    private static void Main()
    {
        _lines = File.ReadAllLines("tree.txt");

        Console.WriteLine(_lines[0]);

        _lineIndex = 1;

        PrintTree();

        Console.ReadLine();
    }

    private static void PrintTree()
    {
        while (_lineIndex < _lines.Length)
        {
            var line = _lines[_lineIndex].Trim();

            if (line == "")
            {
                _lineIndex++;
                continue;
            }

            if (line == "END")
            {
                _depth--;

                _lineIndex++;

                return;
            }

            if (line.StartsWith("BEGIN "))
            {
                var name = line.Substring(6);

                Console.Write(GetIndent());
                Console.WriteLine("├─" + name);

                _depth++;

                PrintTree();
            }

            _lineIndex++;
        }
    }

    private static string GetIndent()
    {
        var result = "";

        for (var i = 0; i < _depth; i++)
        {
            result += "│ ";
        }

        return result;
    }
}
```
</details>


# Лабораторная работа 8. Редактор карт для игры

## Сложность

3 / 5

## Темы

Класс, объект, структура, ссылочный тип данных, структурный тип данных, отличие структуры от класса, передача структуры в метод, методы.

## Задание

Вы разрабатываете редактор карт для небольшой двумерной игры.

Карта состоит из отдельных клеток. Каждая клетка хранит информацию о своём типе. Например, это может быть земля, вода, стена, лава, трава или другой тип поверхности.

Кроме карты, в игре присутствует игрок, которого можно размещать на карте и перемещать между клетками.

Самостоятельно определите, какие сущности следует реализовать в виде классов, а какие — в виде структур. При выборе учитывайте не только состав данных, но и особенности их использования в программе.

Программа должна поддерживать следующие возможности:

- создание новой карты заданного размера;
- изменение типа отдельных клеток;
- отображение карты в консоли;
- размещение игрока на карте;
- перемещение игрока по карте;
- сохранение текущего состояния карты;
- восстановление ранее сохранённого состояния.

После завершения реализации программы проведите несколько экспериментов.

Измените карту после создания резервной копии и убедитесь, что восстановление действительно возвращает карту к сохранённому состоянию.

Также проверьте, что после вызова методов перемещения игрок действительно изменяет своё положение на карте.

Если результат работы программы отличается от ожидаемого, определите причину с помощью отладчика и при необходимости пересмотрите выбор между классом и структурой.

## Подсказки

<details>
<summary>Показать подсказки</summary>

- Подумайте, какие сущности являются самостоятельными объектами, а какие представляют собой лишь небольшое описание состояния.
- При реализации сохранения карты обратите внимание, действительно ли создаётся независимая копия данных.
- Если изменения неожиданно появляются сразу в нескольких местах программы, попробуйте выяснить, используются ли одни и те же объекты повторно.
- Если после вызова метода изменения "исчезают", поставьте точку останова до вызова метода, внутри метода и после возврата из него. Сравните значения переменных.
- Используйте окна **Watch**, **Locals** и **Call Stack** для анализа поведения программы.

### Один из возможных вариантов работы программы

После запуска программа может вывести список доступных команд.

```text
1 - Показать карту
2 - Изменить клетку
3 - Переместить игрока
4 - Создать снимок карты
5 - Восстановить снимок
0 - Выход
```

После выбора команды программа может запрашивать дополнительные данные.

Например, изменение клетки может выглядеть так:

```text
Введите координату X: 3
Введите координату Y: 5

Выберите новый тип клетки:
1 - Земля
2 - Стена
3 - Вода
4 - Лава

> 2
```

Перемещение игрока:

```text
Введите новые координаты игрока:
X: 4
Y: 2
```

---

### Один из возможных вариантов отображения карты

Например, разные типы клеток можно обозначить разными символами.

```text
. - земля
# - стена
~ - вода
^ - лава
P - игрок
```

Тогда карта может выглядеть так:

```text
##########
#........#
#..P.....#
#...~~~..#
#........#
##########
```

Игрок может отображаться поверх клетки, на которой находится.

---

### Один из возможных вариантов реализации снимка

При создании снимка карта должна сохраниться в отдельную переменную.

После этого можно изменить несколько клеток.

Например:

```text
##########
#........#
#..P.....#
##########
```

↓

Изменить несколько клеток

↓

```text
##########
#..^^^^..#
#..P.~~..#
##########
```

↓

Выполнить восстановление снимка

↓

```text
##########
#........#
#..P.....#
##########
```

Если после восстановления карта не вернулась к первоначальному состоянию или восстановились уже изменённые клетки, попробуйте выяснить причину.

---

Во время разработки рекомендуется как можно чаще выводить текущее состояние карты на экран. Это значительно упрощает поиск ошибок.


</details>

## Дополнительное задание

Добавьте возможность сохранить несколько контрольных точек состояния карты и переключаться между ними.

После реализации проверьте, что изменение текущей карты не приводит к изменению ранее сохранённых состояний.