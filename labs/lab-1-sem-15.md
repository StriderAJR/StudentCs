# Лабораторная работа 15. Улучшение библиотеки игровых утилит

## Сложность

3 / 5

## Темы

Статический класс, статические методы, статические поля, методы расширения.

## Задание

Во время разработки игрового движка один из разработчиков создал библиотеку вспомогательных методов. Она полностью работает и активно используется в проекте.

Однако со временем стало понятно, что пользоваться этой библиотекой неудобно. Во многих местах программы приходится передавать один и тот же объект первым параметром метода, из-за чего код стал менее читаемым.

Вам необходимо проанализировать существующую библиотеку и улучшить её интерфейс, не изменяя её функциональность.

Самостоятельно определите:

- какие методы стоит оставить обычными статическими;
- какие методы удобнее превратить в методы расширения;
- какие данные логично хранить в статических полях.

После завершения работы код программы должен стать более понятным и естественным для чтения.

## Подсказки

<details>
<summary>Показать подсказки</summary>

### Вам дана библиотека игровых утилит

Например:

```csharp
public static class PlayerUtils
{
    public static bool IsDead(Player player)
    {
        ...
    }

    public static void Heal(Player player, int amount)
    {
        ...
    }

    public static bool HasEnoughGold(Player player, int amount)
    {
        ...
    }
}

public static class InventoryUtils
{
    public static bool IsEmpty(Inventory inventory)
    {
        ...
    }

    public static Item? FindItem(Inventory inventory, string name)
    {
        ...
    }
}

public static class MapUtils
{
    public static bool IsInside(GameMap map, int x, int y)
    {
        ...
    }

    public static double Distance(GameObject first, GameObject second)
    {
        ...
    }
}
```

---

### В проекте библиотека используется следующим образом

```csharp
if (PlayerUtils.IsDead(player))
{
    ...
}

PlayerUtils.Heal(player, 50);

if (InventoryUtils.IsEmpty(inventory))
{
    ...
}

if (MapUtils.IsInside(map, x, y))
{
    ...
}
```

Подумайте, все ли подобные вызовы одинаково удобно читать.

Можно ли сделать некоторые из них более естественными, не изменяя их поведения?

---

### Статические поля

Некоторые значения используются практически во всём проекте.

Например:

- максимальный размер стопки предметов;
- размер клетки игрового мира;
- стандартный размер карты;
- максимальный уровень персонажа.

Подумайте, где лучше хранить подобные данные.

</details>

## Дополнительное задание

Добавьте в библиотеку несколько новых возможностей.

Например:

- вычисление расстояния между двумя объектами;
- проверка, находится ли игрок рядом с объектом;
- проверка, заполнен ли инвентарь;
- восстановление здоровья до максимального значения;
- получение случайной свободной клетки карты.

При проектировании новых методов постарайтесь сразу выбрать наиболее удобный способ их использования.

После завершения работы сравните читаемость старого и нового вариантов программы и оцените, удалось ли сделать код более понятным без изменения его функциональности.
